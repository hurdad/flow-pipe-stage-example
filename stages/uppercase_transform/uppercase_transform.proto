syntax = "proto3";

package flowpipe.stages.uppercase.v1;

// Configuration for NoopTransform stage
message UppercaseTransformConfig {
  // If true, logs every payload processed
  bool verbose = 1;
}
